spring.application.name=Murza
spring.datasource.url=jdbc:mysql://localhost:3307/engine
spring.datasource.username=root
spring.datasource.password=root1
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true
spring.sql.init.mode=always
server.port=8080

spring.flyway.baseline-on-migrate=true
spring.flyway.enabled=false

jwt.secret=f07634d0e1ef6b6eb89e792ebaf5a57e4ba39438b0a1b48be91e464b64c9f249
jwt.expiration=86400000
jwt.refresh-expiration=604800000

credentials.super.admin.password=12345678
credentials.super.admin.email=super.admin@gmail.com

spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
spring.servlet.multipart.enabled=true

verification-status.inspiration-code=2

cloud.aws.credentials.accessKey=${AWS_ACCESS_KEY_ID}
cloud.aws.credentials.secretKey=${AWS_SECRET_ACCESS_KEY}
cloud.aws.region=${AWS_REGION}
s3.bucket.name=${S3_BUCKET_NAME}


spring.cache.type=redis
spring.data.redis.host=localhost
spring.data.redis.port=6379

app.email.from=${MAIL_SENDER_HOST}
spring.mail.host=${MAIL_SENDER_HOST}
spring.mail.port=587
spring.mail.protocol=smtp
spring.mail.username=${MAIL_SENDER_ACCESS_USERNAME}
spring.mail.password=${MAIL_SENDER_ACCESS_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=${MAIL_SENDER_HOST}

twilio.phone-number=${TWILIO_SMS_SENDER_PHONE}
twilio.account-sid:${TWILIO_SMS_SENDER_SID}
twilio.auth-token: ${TWILIO_SMS_SENDER_TOKEN}
